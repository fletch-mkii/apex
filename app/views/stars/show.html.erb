<div class="overlay-container">
  <div class="row">
    <div class="overlay small-centered small-12 columns">
      <%counter = 0%>
      <div class="row all-attributes text-left">
        <div class="small-4 columns">
        <%@star.attributes.each do |attribute,value|%>
          <%next if attribute == "id"%>
              <li class="<%= 'star-attribute ' + attribute.titleize.delete(' ').downcase %>" >
              <%unless value.nil? || value == 0.0%>
                <%=attribute.titleize + ": #{value} " + @star_units[counter]%>
              <%else%>
                <%=attribute.titleize + ": Unknown"%>
              <%end%>
              </li>
          <%counter += 1%>
        <%end%>
        </div>
        <div class="small-4 columns">
          <% Star::ATTRIBUTE_INFO.each do |key,value| %>
            <div class="<%= key.to_s + ' description animated zoomIn' %>">
              <%= value %>
            </div>
          <%end%>
        </div>
      </div>
  </div>
  <h1>
    Planets That Orbit This Star
  </h1>
  <ul class="row overlay-container accordion" data-accordion role="tablist" data-multi-expand="true" data-allow-all-closed="true">
    <% @planets.each do |planet| %>
      <li class="accordion-navigation text-left">
        <a href=<%="#panel#{planet.id}"%> role="tab" class="accordion-title text-left" id=<%="panel#{planet.id}-heading"%> aria-controls=<%="panel#{planet.id}"%>><h4><%= planet.planet_name %></h4></a>
        <div id=<%="panel#{planet.id}"%> class="accordion-content" role="tabpanel" data-tab-content aria-labelledby=<%="panel#{planet.id}"%>>
          <h5 class="transit text-center"><strong><%= planet.transits? %></strong></h5>
          <div class="row all-attributes text-left">
            <div class="small-4 columns">
              <ul>
              <%counter = 0%>
              <%planet.attributes.each do |attribute,value|%>
                  <%next if attribute == "id" || attribute == "star_id" || attribute == "planet_name" || attribute == "transits"%>
                  <li class="<%= 'attribute planet' + attribute.titleize.delete(' ').downcase %>" >
                  <%unless value.nil? || value == 0.0%>
                    <%=attribute.titleize + ": #{value} " + @planet_units[counter]%>
                  <%else%>
                    <%=attribute.titleize + ": Unknown"%>
                  <%end%>
                  </li>
                  <%counter += 1%>
              <%end%>
              </ul>
            </div>
            <div class="small-8 columns">
              <% Planet::ATTRIBUTE_INFO.each do |key,value| %>
                <div class="<%= key.to_s + ' description animated zoomIn' %>">
                  <%= value %>
                </div>
              <%end%>
            </div>
            <div class="small-8 columns description transit-desc animated slideInDown">
              Transit info yaaaaaaaayyyyy!!!!!!!
            </div>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
</div>
